
{% if options.enableSubModule %}  
  const backTo{{options.parentModuleItemName}} = (item) => {
    document.location.hash = `{{options.parentModuleRoutePath}}/page/${lastParentPage}`
  }
{% endif %}